import assert from 'assert';
// import * from '../lib/index';

describe('Test the weather webhook', () => {
  describe('callWeatherApi function', () => {
    it('should...', () => {
      assert.equal([1, 2, 3].indexOf(4), -1);
    });
  });

  describe('weatherWebhook function', () => {
    it('should...', () => {
      assert.equal([1, 2, 3].indexOf(4), -1);
    });
  });
});
